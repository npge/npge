pipe S {
    add AddBlocks target=other;
    add AddBlastBlocks --blast-min-length=100 target=hits other=other;
    add OverlaplessUnion target=target other=hits;
    add OutputPipe --skip-rest:=1;
};
run S;

