#! ${BRMETA}

pipe AddBlastPipe {
    name "Add blocks found by blast";
    add AddBlocks --import-alignment:=1;
    add AddBlastBlocks other=target target=target;
    add CleanUp;
    add CheckNoOverlaps;
    add OutputPipe;
};

run AddBlastPipe;

