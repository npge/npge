#! ${BRMETA}

pipe KeepNonoverlapping {
    name "Filter blocks and keep only non-overlapping ones";
    add AddBlocks target=other;
    add Filter target=other;
    add OverlaplessUnion;
    add OutputPipe;
};

run KeepNonoverlapping;

