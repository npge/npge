#! ${BRMETA}

pipe IsPangenomePipe {
    name "Test pangenome";
    add AddBlocks --import-alignment=1;
    add IsPangenome blast-hits=blast-hits;
    add Output target=blast-hits --out-file=/dev/null;
};

run IsPangenomePipe;

