#! ${BRMETA}

pipe OverlapsResolverPipe {
    name "Resolve overlaps";
    add AddBlocks;
    add Connector;
    add OverlapsResolver;
    add CheckNoOverlaps;
    add OutputPipe;
};

run OverlapsResolverPipe;

