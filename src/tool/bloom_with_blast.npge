#! ${NPGE}

pipe BloomWithBlastPipe {
    name "Find and expand anchors, import blast hits";
    add AddBlocks;
    add AnchorFinder;
    add CleanUp;
    add MetaAligner;
    add AddBlastBlocks;
    add CleanUp;
    add CheckNoOverlaps;
    add OutputPipe;
};

run BloomWithBlastPipe;

