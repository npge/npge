#! ${BRMETA}

pipe X {
    add AddBlocks;
    add AddBlastBlocks target=hits other=target;
    add Filter;
    add Output target=hits prefix|hits-;
    add OverlaplessUnion target=target other=hits;
    add ExternalAligner;
    add Filter;
    add OutputPipe;
};

run X;

