#! ${BRMETA}

pipe GapsIter {
    max_loops -1;
    add MoveGaps;
    add CutGaps;
};

pipe Align {
    add AddBlocks;
    add ExternalAligner;
    add GapsIter;
    add Filter;
    add OutputPipe;
};

run Align;

