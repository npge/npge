#! ${BRMETA}

pipe ImportBlastHitsPipe {
    name "Import blast hits";
    add AddBlocks target=other --import-alignment:=1;
    add ImportBlastHits;
    add OutputPipe;
};

run ImportBlastHitsPipe;

