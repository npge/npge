#! ${NPGE}

pipe BloomRepeatsPipe {
    name "Find and expand anchors";
    add AddBlocks;
    add AnchorFinder;
    add CleanUp;
    add CheckNoOverlaps;
    add OutputPipe;
};

run BloomRepeatsPipe;

