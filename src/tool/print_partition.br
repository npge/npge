#! ${BRMETA}

pipe X {
    add AddBlocks target=other prefix|pangenome-;
    add SequencesFromOther target=target other=other;
    add AddBlocks target=target prefix|genes-;
    add PrintPartition;
};

run X;

