${PROJECT_BINARY_DIR}/src/test/rand_seq > test.fasta &&
${PROJECT_BINARY_DIR}/src/find_anchors test.fasta > repeats.txt &&
sed ':a;N;$!ba;s/\n//g' test.fasta > for_grep.txt &&
for i in `cat repeats.txt`; do
    if [ `grep -o $i for_grep.txt | wc -l` -lt 1 ]; then
        exit 1
    fi
done

